import React from 'react'

//Exporting this may be more difficult to isolate than the handleChange - bc we have diffent
// types of submits (from new Reminder, from Update Reminder, etc.) where we may want different outcomes

//This is still possible and potentially a good use of a utility function

export const handleSubmit = () => {
  return (
    <div>handleSubmit</div>
    
  )
}
